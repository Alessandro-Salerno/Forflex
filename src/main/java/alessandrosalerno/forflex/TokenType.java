package alessandrosalerno.forflex;

public enum TokenType {
    PLUS,
    MINUS,
    STAR,
    SLASH,
    LPAREN,
    RPAREN,
    IDENTIFIER,
    NUMBER,
    STRING,
    COMMA,
    EOF
}
